import colorScheme from './color-scheme.mjs';
import gapUtility from './gap.mjs';
import noSpinnerUtility from './no-spinner.mjs';

/*
  Add each plugin to the array in the order they
  should appear in the cascade.
*/
export default [colorScheme, gapUtility, noSpinnerUtility];
